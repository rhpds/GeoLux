FROM node:22-alpine

WORKDIR /app

COPY frontend/package.json frontend/package-lock.json* ./
RUN npm ci --ignore-scripts 2>/dev/null || npm install --ignore-scripts

COPY frontend/ .

ENV GEOLUX_API_URL=http://localhost:8091

EXPOSE 3001

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
